---
import Arrow from "./Arrow.astro";
---

<button
  type="button"
  data-back-to-top
  aria-label="Revenir en haut de la page"
  class="invisible fixed bottom-6 right-6 z-40 flex size-11 translate-y-2 items-center justify-center rounded-full border border-zinc-800 bg-zinc-900/90 text-zinc-400 opacity-0 backdrop-blur transition hover:border-accent/40 hover:text-accent data-[visible]:visible data-[visible]:translate-y-0 data-[visible]:opacity-100"
>
  <Arrow direction="up" />
</button>

<script>
  import { wireBackToTop } from "../../lib/back-to-top";
  import { onEachPage } from "../../lib/enhance";

  /**
   * Visibilité et retour en haut : voir src/lib/back-to-top.ts, où ils sont
   * testés. Il ne reste ici qu'à trouver le bouton.
   */
  onEachPage((signal) => {
    const button = document.querySelector<HTMLButtonElement>("[data-back-to-top]");

    if (button) wireBackToTop(button, window, signal);
  });
</script>
